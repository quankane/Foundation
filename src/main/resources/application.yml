server:
  port: 8080

spring:
  application:
    name: restaurant-ordering-webapp
  profiles:
    active: @spring.profiles.active@

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    root: info
    web: info